$? $@ $# $*

$@, $*  to provide multiple params
but $* wont consider the quotes and uses as individual while parsig args in to script 
$# to count the number of parms given as args
$? is the command success or failuer 
$1 $2 $3 individual 
for; do; logic; done
if condition, else condition then fi

shell script ifs $@

shell operators

Arthimatic 
relational
Boolean
String
File test

STDIN:0
STDOUT:1
STDERR:2
/dev/null
2>/dev/null to redirect output

bash stdtesting 1>/dev/null - Redirect stdout to /dev/null
bash stdtesting 2>/dev/null - Redirect stderr to /dev/null
bash stdtesting >/dev/null 2>&1 - Redirect stderr and stdout to /dev/null

tee command to redirect and display on consle

python paramico for ssh

cat /etc/passwd | tee /tmp/log1
cat /etc/passwd | tee -a /tmp/log1

find - file name, sizes
sed vs tr -- replaces strings & words
awk - data processing
regex


installing jenkins on ubuntu
apt update && apt install -y openjdk-8-jdk
apt update && apt install -y openjdk-11-jdk
jenkins.io ssite
